## 1. 数据迁移基础能力

- [x] 1.1 为全量导出、目录范围收集、顶层父 ID 归一化和空目录导出编写失败优先的单元测试
- [x] 1.2 新增 `bookmark-transfer` 纯函数模块，实现全量 payload 与当前目录自包含 payload 的创建
- [x] 1.3 为全部节点 ID 重映射、内部父 ID 重连、目标目录挂载、重复导入和顺序追加编写失败优先的单元测试
- [x] 1.4 实现导入转换函数，为每个节点生成新 ID、保留内容及时间元数据，并计算目标目录追加顺序
- [x] 1.5 为无效目标目录、校验失败零写入和事务失败整体回滚编写 IndexedDB 测试
- [x] 1.6 在 IndexedDB 数据层实现目标目录复验与单事务批量 `add`，并保留旧 v1 JSON 格式兼容
- [x] 1.7 移除界面对整库覆盖导入的依赖，并在确认无调用后删除或降级封装 `replaceAllNodes`

## 2. 收藏排序

- [x] 2.1 为默认、时间正倒序、标题正倒序、稳定回退和未知配置回退编写排序单元测试
- [x] 2.2 定义排序类型、比较器和 `webwings:bookmark-sort` 本地持久化 hook
- [x] 2.3 在收藏页工具栏增加五种排序选项，并按“范围筛选、搜索筛选、排序”顺序生成可见列表
- [x] 2.4 验证切换排序不会修改目录树、IndexedDB 节点顺序或其他目录内容

## 3. 工作区导航与菜单修复

- [x] 3.1 将 `FolderTree` 从 `App.tsx` 拆出，并把目录“三个点”菜单改为触发按钮下方右对齐且支持边缘碰撞调整
- [x] 3.2 增加 `bookmarks/settings` 轻量视图状态，将左下角入口替换为带选中状态的“设置”按钮
- [x] 3.3 实现右侧 `SettingsView`，首期仅展示数据管理说明、“导出全部”和“导入”操作
- [x] 3.4 确保用户从设置页点击全部收藏、未分类或任意目录时返回对应收藏视图，并保留目录展开状态

## 4. 导入导出界面

- [x] 4.1 实现可复用 `ImportDialog`，支持文件选择、文件摘要、根目录/目录选择、锁定目标目录、错误状态和提交状态
- [x] 4.2 将设置页“导出全部”连接到全量 payload 下载，并将设置页“导入”连接到可选择目标的合并导入
- [x] 4.3 在真实目录页面右上角菜单增加“导出当前目录”和“导入到当前目录”，并在全部收藏、未分类和设置页隐藏目录级操作
- [x] 4.4 导入成功后重新加载节点、更新目录计数并显示导入数量；导入失败时保留现有数据并显示可理解的错误

## 5. 验证与回归

- [x] 5.1 扩充数据层测试，覆盖旧 v1 全量文件、目录文件、重复导入、不安全 URL、缺失父目录和循环目录
- [x] 5.2 运行全部 Vitest 测试、TypeScript 检查和 Vite 生产构建并修复所有失败
- [x] 5.3 在 760×580 扩展弹窗中验证目录菜单定位、设置/收藏切换、五种排序和空状态布局
- [ ] 5.4 在真实浏览器扩展环境中端到端验证导出全部、目录导出、选择目标导入、当前目录导入和重复导入后的层级与 ID 唯一性
  - 阻塞：自动化浏览器安全策略禁止访问 `chrome://extensions` / `edge://extensions`；需人工加载 `dist` 后复核。
- [x] 5.5 更新 README，说明设置入口、排序选项、目录级导入导出以及导入采用合并和 ID 重映射语义
